#messageOutput {
  @apply bg-yellow-50;
  border: 1px solid #ccc;
  border-radius: 4px;
  padding: 1rem;
}

.btn {
  @apply font-bold py-2 px-4 rounded;
}
.btn-primary {
  @apply bg-blue-500 text-white;
}
.btn-primary:hover {
  @apply bg-blue-700;
}
